{% for action in actions %}
    {% include "layouts/_action.html.twig" %}
{% endfor %}

<div class="action-container">
    <button data-ajax-url="{{ routeTripDay('action.add', trip.id, day.id) }}" class="btn btn-primary action-add-modal-btn">
        <i class="fa fa-thumb-tack"></i> {{ t('pages.trip.show.add-action') }}
    </button>
</div>